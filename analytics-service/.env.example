# Server Configuration
PORT=8008

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=insavein
DB_SSLMODE=disable

# Database Read Replica (optional, for read-heavy operations)
DB_REPLICA_HOST=localhost

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
